---
// AboutMetrics.astro - สถิติความสำเร็จเชิงประจักษ์ 4 ด้านของค่าย
export interface Metric {
  num: string;
  title: string;
  desc: string;
}

export interface Props {
  kicker: string;
  meta: string;
  metrics: Metric[];
}

const { kicker, meta, metrics } = Astro.props;
---

<div class="editorial-col metrics-col">
  <div class="kicker-header-row">
    <span class="editorial-kicker">
      <span class="kicker-pip"></span>{kicker}
    </span>
  </div>
  
  <div class="metrics-list">
    {metrics.map((metric) => (
      <div class="metric-row">
        <span class="metric-num">{metric.num}</span>
        <div class="metric-content">
          <h3>{metric.title}</h3>
          <p>{metric.desc}</p>
        </div>
      </div>
    ))}
  </div>
</div>
